# Puerto en el que escucha el servidor Express
PORT=3000

# Ruta absoluta al fichero SQLite persistente (debe vivir en disco persistente
# en producción; si el hosting borra el disco entre despliegues se pierden
# todos los datos, ver research.md punto 6)
DB_PATH=./data/presupuestospro.db
